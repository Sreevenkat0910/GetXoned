services:
  - type: static
    name: xoned-frontend
    buildCommand: npm install && npm run build
    staticPublishPath: ./dist
    envVars:
      - key: VITE_API_URL
        value: https://xoned-backend.onrender.com
      - key: VITE_CLERK_PUBLISHABLE_KEY
        sync: false
